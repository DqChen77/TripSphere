spring:
  data:
    mongodb:
      uri: mongodb://root:fudanse@localhost:27017
      database: itinerary_test_db

# Disable Nacos for tests
  cloud:
    nacos:
      discovery:
        enabled: false

logging:
  level:
    org.tripsphere.itinerary: DEBUG
    org.springframework.data.mongodb: DEBUG
